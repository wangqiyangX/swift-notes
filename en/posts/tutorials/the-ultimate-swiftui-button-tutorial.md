# The ultimate SwiftUI button tutorial

## Build a button

SwiftUI supported many meotheds to create a Button.

```swift{3-5}
struct ContentView: View {
    var body: some View {
        Button("Click") {
            print("Clicked")
        }
    }
}
```
